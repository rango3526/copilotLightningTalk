{
    "dependencies": {
        "jest": "^29.3.1",
        "node-fetch": "^2.6.7"
    },
    "devDependencies": {
        "@types/jest": "^26.0.24",
        "ts-jest": "^29.0.0",
        "typescript": "^4.4.3"
    },
    "scripts": {
        "test": "jest"
    },
    "jest": {
        "preset": "ts-jest",
        "testEnvironment": "node",
        "testMatch": [
            "**/tests/**/*.test.ts"
        ]
    }
}
